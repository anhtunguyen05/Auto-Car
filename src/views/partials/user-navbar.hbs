<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/user">
            <i class="bi bi-car-front-fill"></i> Car Rental
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{#if isHome}}active{{/if}}" href="/user">
                        <i class="bi bi-house-door"></i> Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isBrowseCars}}active{{/if}}" href="/user/browse-cars">
                        <i class="bi bi-car-front"></i> Browse Cars
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isMyBookings}}active{{/if}}" href="/user/my-bookings">
                        <i class="bi bi-calendar-check"></i> My Bookings
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle"></i> {{#if currentUser}}{{currentUser.name}}{{else}}Account{{/if}}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        {{#if currentUser}}
                        <li class="dropdown-item-text">
                            <small class="text-muted">{{currentUser.email}}</small>
                        </li>
                        <li class="dropdown-item-text">
                            <small class="badge bg-success">Customer</small>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        {{/if}}
                        <li><a class="dropdown-item" href="/user/profile">
                            <i class="bi bi-person"></i> My Profile
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/auth/logout">
                            <i class="bi bi-box-arrow-right"></i> Sign out
                        </a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
