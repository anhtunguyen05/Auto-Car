<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/owner">
            <i class="bi bi-building"></i> Car Owner Portal
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{#if isHome}}active{{/if}}" href="/owner">
                        <i class="bi bi-house-door"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isMyCars}}active{{/if}}" href="/owner/my-cars">
                        <i class="bi bi-car-front-fill"></i> My Cars
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isAddCar}}active{{/if}}" href="/owner/add-car">
                        <i class="bi bi-plus-circle"></i> Add Car
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isBookings}}active{{/if}}" href="/owner/bookings">
                        <i class="bi bi-calendar-check"></i> Bookings
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle"></i> {{#if currentUser}}{{currentUser.name}}{{else}}Account{{/if}}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        {{#if currentUser}}
                        <li class="dropdown-item-text">
                            <small class="text-muted">{{currentUser.email}}</small>
                        </li>
                        <li class="dropdown-item-text">
                            <small class="badge bg-primary">Car Owner</small>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        {{/if}}
                        <li><a class="dropdown-item" href="/owner/profile">
                            <i class="bi bi-person"></i> My Profile
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="/auth/logout">
                            <i class="bi bi-box-arrow-right"></i> Sign out
                        </a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
